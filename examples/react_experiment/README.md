# react_experiment example

Goal: demonstrate the ReAct (tool-using) loop.

## Run

```bash
cd examples/react_experiment

go run main.go
```

## Env

- `OPENAI_API_KEY` or `OPENROUTER_API_KEY`

## Optional

- `DSGO_LOG=pretty` for more verbose logs
