# Scripts

Minimal index of the scripts shipped in this repo.

| Path | Purpose |
|---|---|
| `scripts/check-eof.sh` | Verify files end with a trailing newline |
| `scripts/fix_fmt.sh` | Format all Go files |
| `scripts/install-hooks.sh` | Install git hooks |
| `scripts/pre-commit` | Pre-commit hook (runs repo checks) |
